<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alexandra.fixacao.com.usuariofixacao">

    <uses-permission android:name="android.permission.INTERNET"/> <!-- permissão para acesso a Internet -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaLogado"/>
        <activity android:name=".TelaCadastro"/>
        <activity android:name=".TelaListar"/>
        <activity android:name=".TelaEditar"/>
    </application>

</manifest>